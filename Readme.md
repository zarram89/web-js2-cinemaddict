# Проект «{{projectTitle}}»

* Студент: [{{userName}}]({{userProfile}}).
* Наставник: `Неизвестно`.

---

_Не удаляйте и не изменяйте папки и файлы:_
_`.editorconfig`, `.gitattributes`._

---

### Памятка

#### 1. Зарегистрируйтесь на Гитхабе

Если у вас ещё нет аккаунта на [github.com](https://github.com/join), скорее зарегистрируйтесь.

#### 2. Создайте форк

Откройте репозиторий и нажмите кнопку «Fork» в правом верхнем углу. Репозиторий из Академии будет скопирован в ваш аккаунт.

<img width="769" alt="Press 'Fork'" src="https://cloud.githubusercontent.com/assets/259739/20264045/a1ddbf40-aa7a-11e6-9a1a-724a1c0123c8.png">

Получится вот так:

<img width="769" alt="Forked" src="https://cloud.githubusercontent.com/assets/259739/20264122/f63219a6-aa7a-11e6-945a-89818fc7c014.png">

#### 3. Клонируйте репозиторий на свой компьютер

Будьте внимательны: нужно клонировать свой репозиторий (форк), а не репозиторий Академии. Также обратите внимание, что клонировать репозиторий нужно через SSH, а не через HTTPS. Нажмите зелёную кнопку в правой части экрана, чтобы скопировать SSH-адрес вашего репозитория:

<img width="769" alt="SSH" src="https://cloud.githubusercontent.com/assets/259739/20264180/42704126-aa7b-11e6-9ab4-73372b812a53.png">

Клонировать репозиторий можно так:

```
git clone SSH-адрес_вашего_форка
```

Команда клонирует репозиторий на ваш компьютер и подготовит всё необходимое для старта работы.

#### 4. Начинайте обучение!

---

<a href="https://htmlacademy.ru/intensive/ecmascript"><img align="left" width="50" height="50" title="HTML Academy" src="https://up.htmlacademy.ru/static/img/intensive/ecmascript/logo-for-github.svg"></a>

Репозиторий создан для обучения на профессиональном курсе «[JavaScript. Архитектура клиентских приложений](https://htmlacademy.ru/intensive/ecmascript)» от [HTML Academy](https://htmlacademy.ru).

[check-image]: https://github.com/htmlacademy-ecmascript/{{userId}}-{{projectName}}/workflows/Project%20check/badge.svg?branch=master
[check-url]: https://github.com/htmlacademy-ecmascript/{{userId}}-{{projectName}}/actions

Вот тот же текст, **аккуратно переведённый в Markdown**, без изменения смысла и с нормальной структурой 👇

---

# 1.3 — Время разбивать камни

## Архив проекта

Динамический сайт от статичного отличает то, что части страницы перерисовываются на лету с помощью JavaScript. Перерисовывать будет проще, если разбить цельную страницу на независимые части — **компоненты**.

Пословица «Семь раз отмерь, один отрежь» здесь тоже работает, поэтому прежде, чем что-то разбивать, давайте посмотрим на готовые (цельные) страницы в директории `/markup`.

Смысл в том, чтобы глазами выделить изменяемые части от статичных. В будущем каждая из таких «изменяемых» частей станет отдельным компонентом.

---

## Анализ страниц

### Главная страница `list.html`

Если сравнить главную страницу:

* **Главная страница**

и страницу без дополнительных блоков:

* **Страница без дополнительных блоков (`list-no-extra.html`)**

появляются первые два компонента:

* **Top rated**
* **Most commented**

---

### Страница загрузки

Если посмотреть страницу загрузки, становится понятно, что:

* список фильмов — это тоже компонент

---

### Страница с фильтрами

После просмотра страницы с применёнными фильтрами `list-filter.html` становится ясно, что:

* карточка фильма — самостоятельный компонент
* блоки **Top rated**, **Most commented** и список — это контейнеры для отрисовки

---

## Итоговый список компонентов

Изучив все страницы и техническое задание (чтобы понять, какие части страницы меняются), можно составить список компонентов:

* **Карточка**

  * Описание
  * Кнопки
* **Попап**

  * Описание
  * Кнопки
  * Список комментариев
  * Форма добавления комментария
* **Кнопка «Показать ещё»**
* **Статистика** (в подвале)
* **Звание пользователя** (в шапке)
* **Фильтры**
* **Сортировка**
* **Список** (универсальный компонент для списка фильмов и блоков *Top rated*, *Most commented*)
* **Фильмы** (общий список)
* **Контент страницы**

---

## Компоненты с точки зрения JavaScript

Компоненты — это **отдельные файлы**.
Под каждый компонент создаётся свой файл.

Так как мы используем архитектурный паттерн **MVP**, а компоненты ближе всего к представлению (**View**), все файлы складываются в директорию:

```
/src/view
```

---

## Базовая структура View

Каждый компонент — это:

* HTML-разметка
* JavaScript для превращения HTML в DOM-элемент

Удобно, когда все View похожи по структуре. Возьмём базовый шаблон из учебного проекта:

```js
// Вспомогательная функция для превращения строки с HTML в DOM-элементы
import {createElement} from '../render.js';

// Функция, которая будет возвращать строку с HTML-разметкой компонента
const createTemplate = () => '...';

// Класс — он же сам компонент
export default class View {
  getTemplate() {
    return createTemplate();
    // Можно не создавать отдельную функцию,
    // а сразу вернуть HTML из метода
  }

  getElement() {
    if (!this.element) {
      this.element = createElement(this.getTemplate());
    }

    return this.element;
  }

  removeElement() {
    this.element = null;
  }
}
```

Для удобства стоит называть классы и функции менее абстрактно:

* `FilmButtonMoreView`
* `createFilmButtonMoreTemplate`

---

## Разметка компонентов

Разметку берём из директории `/markup`.

Пока используем **статичную разметку** с данными.

В итоге получится несколько файлов компонентов, похожих друг на друга по структуре.

---

## Отрисовка View на странице

Начнём с простых компонентов:

* звание пользователя
* статистика в подвале

### Функция render

```js
const render = (component, container, place = RenderPosition.BEFOREEND) => {
  container.insertAdjacentElement(place, component.element);
};
```

Аргументы:

1. `component` — экземпляр View
2. `container` — DOM-элемент-контейнер
3. `place` — позиция вставки (по умолчанию `BEFOREEND`)

---

### Пример: звание пользователя

1. В `createTemplate` используем разметку `section.header__profile`
2. Контейнер — `header.header`
3. Позиция — `RenderPosition.BEFOREEND`

#### Точка входа:

```js
import HeaderProfileView from './view/header-profile-view.js';
import {render} from './render.js';

const siteHeaderElement = document.querySelector('.header');

render(new HeaderProfileView(), siteHeaderElement);
```

Запускаем:

```bash
npm run start
```

Результат — компонент на своём месте.

---

## Фильтры и статистика

Аналогично отрисовываем:

* фильтры
* статистику в подвале

Результат — всё отображается корректно.

---

## Презентер и список фильмов

Отрисовка списка фильмов — самая сложная часть.

Список — **мозг приложения**.
В дальнейшем появится много условий:

* что рисовать
* когда рисовать
* что перерисовывать

Поэтому простой `render()` в точке входа не подходит.

---

## Presenter в MVP

Для сложной логики используется **Presenter**.

Презентеры храним в директории:

```
/src/presenter
```

Презентер:

* знает обо всех компонентах списка
* создаёт их экземпляры
* вызывает `render()` сам

Он управляет:

* карточками
* попапом
* кнопкой «Показать ещё»
* сортировкой

---

## Использование презентера

В точке входа:

```js
const filmsPresenter = new FilmsPresenter();

filmsPresenter.init(siteMainElement);
```

Запускаем проект:

```bash
npm run start
```

---

## Итог

Интерфейс пока:

* тесный
* статичный
* «не по-настоящему»

Но это **осознанный фундамент**, который мы будем развивать в следующих заданиях 💪

---

Вот этот текст, **аккуратно переведённый в Markdown**, в том же стиле, что и предыдущий 👇

---

# 2.1 — Шаблонизируй это

## Архив проекта

Цель задания — подготовить наши **View** к работе с настоящими данными, которые в будущем мы будем получать с сервера. Остальное вторично, поэтому не стесняемся подглядывать в техзадание и разметку.

В пункте **1.3** техзадания перечислена вся информация, которая будет приходить с сервера:

* **Информация с сервера**

В пункте **1.8.1** показаны структуры данных:

* **Структуры данных**

Наша задача — повторить их с минимальными затратами, пока мы не научимся работать с сервером.

Решать задачу будем в три приёма:

1. Описать функцию-генератор данных
2. Завести модель (**Model**) в приложении
3. Научить **View** отрисовывать данные извне

---

## Генерация данных

Проще всего начинать не с чистого листа, поэтому первым делом возьмём структуру из техзадания и обернём её в функцию-генератор, предварительно создав файл:

```text
/src/mock/film.js
```

Пример генератора фильма:

```js
const generateFilm = () => ({
  title: "A Little Pony Without The Carpet",
  alternative_title: "Laziness Who Sold Themselves",
  total_rating: 5.3,
  poster: "images/posters/blue-blazes.jpg",
  age_rating: 0,
  director: "Tom Ford",
  writers: [
    "Takeshi Kitano"
  ],
  actors: [
    "Morgan Freeman"
  ],
  release: {
    date: "2019-05-11T00:00:00.000Z",
    release_country: "Finland"
  },
  runtime: 77,
  genre: [
    "Comedy"
  ],
  description: "Oscar-winning film, a war drama about two young people, from the creators of timeless classic \"Nu, Pogodi!\" and \"Alice in Wonderland\", with the best fight scenes since Bruce Lee."
});
```

Формально задача решена, но нас не устраивает ситуация, когда во всём приложении один и тот же фильм. Нам нужны данные, похожие на серверные.

---

## Рандомизация данных

Подход одинаков для всех полей. Рассмотрим на примере `title`.

### 1. Константа с возможными значениями

```js
const titles = [
  'Country On Him',
  'Raiders With The Carpet',
  'Guest Who Sold The Darkness',
  ...
];
```

### 2. Функция получения случайного значения

```js
const getRandomValue = (items) => ...;
```

### 3. Использование в генераторе

```js
const generateFilm = () => ({
  title: getRandomValue(titles),
  alternative_title: "Laziness Who Sold Themselves",
  total_rating: 5.3,
  ...
});
```

### 4. Повторяем для остальных данных

---

## Важные нюансы

### 1. camelCase вместо snake_case

В коде нельзя использовать `snake_case`, поэтому:

* `alternative_title` → `alternativeTitle`
* `total_rating` → `totalRating`

---

### 2. Разные типы случайных данных

Одной функции `getRandomValue()` недостаточно. Понадобятся:

* `getRandomInteger()` — для целых чисел
* `getRandomFloat()` — для дробных

Проще всего взять готовые реализации и не писать их с нуля.

---

### 3. Вложенные структуры

* `writers`, `actors` — массивы строк
* `release` — объект

Вложенные структуры заполняются аналогично, но требуют внимательности.

---

## Расширенная структура фильма

Помимо информации о фильме, нужны ещё поля `id` и `comments`:

```js
{
  id: 0,
  comments: [ ... ],
  filmInfo: { ... },
}
```

---

## Генерация id фильмов

```js
const generateFilms = () => {
  // Создаем массив с данными о фильмах
  const films = Array.from({length: FILM_COUNT}, generateFilm);

  /*
    Преобразуем в нужную структуру:
    {
      id,
      comments,
      filmInfo,
    }
    Пока только id и filmInfo
  */
  return films.map((film, index) => {
    return {
      id: String(index + 1),
      filmInfo: film,
    };
  });
};
```

---

## Генерация комментариев к фильмам

Комментарии — это массив id комментариев.

```js
const generateFilms = () => {
  const films = Array.from({length: FILM_COUNT}, generateFilm);

  // Нужен, чтобы id комментариев не повторялись
  let totalCommentsCount = 0;

  return films.map((film, index) => {
    const filmCommentsCount = getRandomInteger(1, MAX_COMMENTS_ON_FILM);

    totalCommentsCount += filmCommentsCount;

    return {
      id: String(index + 1),
      comments: Array.from(
        {length: filmCommentsCount},
        (_value, commentIndex) => String(totalCommentsCount - commentIndex)
      ),
      filmInfo: film,
    };
  });
};
```

---

## Ситуация без комментариев

Учитываем случай, когда у фильма нет комментариев:

```js
const generateFilms = () => {
  const films = Array.from({length: FILM_COUNT}, generateFilm);

  let totalCommentsCount = 0;

  return films.map((film, index) => {
    const hasComments = getRandomInteger(0, 1);

    const filmCommentsCount = hasComments
      ? getRandomInteger(1, MAX_COMMENTS_ON_FILM)
      : 0;

    totalCommentsCount += filmCommentsCount;

    return {
      id: String(index + 1),
      comments: hasComments
        ? Array.from(
            {length: filmCommentsCount},
            (_value, commentIndex) => String(totalCommentsCount - commentIndex)
          )
        : [],
      filmInfo: film,
    };
  });
};
```

---

## Генерация комментариев

Файл:

```text
/src/mock/comment.js
```

```js
const generateComment = () => ({ ... });

const getCommentCount = (films) =>
  films.reduce((count, film) => count + film.comments.length, 0);

const generateComments = (films) => {
  const commentCount = getCommentCount(films);

  return Array.from({length: commentCount}, (_value, index) => {
    const commentItem = generateComment();

    return {
      id: String(index + 1),
      ...commentItem,
    };
  });
};
```

`getCommentCount()` считает, сколько комментариев нужно создать, исходя из данных фильмов.

---

## Проверка генераторов

В точке входа:

```js
import {generateFilms} from './mock/film';
import {generateComments} from './mock/comment';

const films = generateFilms();
const comments = generateComments(films);

console.log(films);
console.log(comments);
```

Результат — сгенерированные данные в консоли.

---

## Модели (Model)

Модели храним в директории:

```text
/src/model
```

Пример модели фильмов:

```js
import {generateFilms} from '../mock/film.js';

export default class FilmsModel {
  films = generateFilms();

  get = () => this.films;
}
```

---

## Связь Model и Presenter

В точке входа создаём модели и передаём их в презентер:

```js
const filmsModel = new FilmsModel();
const commentsModel = new CommentsModel(filmsModel);

const filmsPresenter = new FilmsPresenter();

filmsPresenter.init(siteMainElement, filmsModel, commentsModel);
```

После этого нужно обновить метод `.init()` в презентере.

---

## Передача данных во View

Компоненты уже получают данные, но пока не умеют их использовать. Нужно:

1. Принять данные в конструкторе
2. Передать их в шаблон

Пример для карточки фильма:

```js
export default class FilmCardView {
  constructor(film) {
    this.film = film;
  }

  getTemplate() {
    return createFilmCardTemplate(this.film);
  }

  getElement() { ... }

  removeElement() { ... }
}
```

---

## Интерполяция данных

Так как `createFilmCardTemplate()` возвращает HTML-строку, данные просто интерполируются в шаблон.

---

## Итог

Запускаем сервер и видим, что при каждой загрузке страницы:

* данные генерируются автоматически
* данные каждый раз разные
* компоненты отображают реальные структуры данных

Готово 🚀

Вот этот текст, **переведённый в аккуратный Markdown**, в том же стиле и уровне детализации 👇

---

# 4.2 — Разделяй и властвуй

## Архив проекта

В программировании редко нужно «изобретать велосипед». Чаще — взять готовый инструмент и хорошо в нём разобраться. Это может быть **React.js**, **Vue.js** или самописный фреймворк — не важно.

В прошлом задании Кекс любезно предоставил нам такой фреймворк, а мы с ним разобрались (если нет — стоит сделать это прямо сейчас). Его цель — максимально снять с нас рутинные задачи:

* реализацию интерфейсов классов
* написание вспомогательных функций
* дублирование однотипного кода

> ⚠️ После получения обновлений от Кекса нужно доустановить новые пакеты:
>
> ```bash
> npm install
> ```

---

## Наследование от AbstractView

Начнём с того, что унаследуем все наши **View** от `AbstractView`.

Благодаря наследованию нам больше не нужно дублировать одни и те же методы и свойства во всех компонентах — от них можно избавиться.

### Сравнение компонента «до» и «после»

#### До

```js
const createFilmButtonMoreTemplate = () =>
  '<button class="films-list__show-more">Show more</button>';

export default class FilmButtonMoreView {
  #element = null;

  get template() {
    return createFilmButtonMoreTemplate();
  }

  get element() {
    if (!this.#element) {
      this.#element = createElement(this.template);
    }

    return this.#element;
  }

  removeElement() {
    this.#element = null;
  }
}
```

#### После

```js
const createFilmButtonMoreTemplate = () =>
  '<button class="films-list__show-more">Show more</button>';

export default class FilmButtonMoreView extends AbstractView {
  get template() {
    return createFilmButtonMoreTemplate();
  }
}
```

---

## Что изменилось

В компонентах осталось (и будет оставаться дальше) **только то, что относится к конкретному компоненту**. Пока что — это только шаблон.

> Больше абстракций богу абстракций.

---

## Добавление логики во View

Теперь, когда наши View стали минималистичными и аккуратными, можно начинать добавлять в них логику. В первую очередь — **обработчики событий**.

Обработку события условно можно разделить на две части:

1. **Работа с DOM** (`addEventListener`)
2. **Бизнес-логика** — то, что должно произойти в ответ на событие

### Разделение ответственности

* **View** → DOM и `addEventListener`
* **Presenter** → логика и принятие решений

Чтобы поддержать это разделение, в `AbstractView` есть механизм подписок.

---

## Новый подход к обработчикам событий

Теперь во всех View должно быть минимум **два метода**:

1. **Публичный** — для передачи логики из Presenter
2. **Приватный** — используется внутри `addEventListener`

Рассмотрим на примере кнопки **«Показать ещё»**.

---

## Как было раньше

Presenter напрямую работал с DOM:

```js
export default class FilmsPresenter {
  ...

  #renderFilmBoard() {
    ...

    if (this.#films.length > FILM_COUNT_PER_STEP) {
      render(this.#filmButtonMoreComponent, this.#filmListComponent.element);

      this.#filmButtonMoreComponent
        .element
        .addEventListener('click', (evt) =>
          this.#filmButtonMoreClickHandler(evt)
        );
    }
  }
}
```

❌ Presenter знает про `.element`
❌ Presenter знает про `addEventListener`

---

## Как стало теперь

Presenter работает **только через интерфейс View**:

```js
export default class FilmsPresenter {
  ...

  #renderFilmBoard() {
    ...

    if (this.#films.length > FILM_COUNT_PER_STEP) {
      render(this.#filmButtonMoreComponent, this.#filmListComponent.element);

      this.#filmButtonMoreComponent
        .setButtonClickHandler(() =>
          this.#filmButtonMoreClickHandler()
        );
    }
  }
}
```

---

## Почему так лучше

Мы чётко разделили ответственность:

* Логика

  ```js
  () => this.#filmButtonMoreClickHandler()
  ```

  остаётся в **Presenter**

* Работа с DOM

  ```js
  .addEventListener('click', ...)
  ```

  остаётся во **View**

Связь между **Presenter** и **View** осуществляется через **чёткий интерфейс**, а не через доступ к DOM напрямую.

---

## Применение подхода

Аналогичным образом нужно заменить прямые вызовы `.addEventListener()`:

* во всех компонентах
* на публичные методы View

---

## Проверка результата

Запускаем сервер разработки и убеждаемся, что:

* приложение работает как раньше
* логика и DOM разделены
* код стал чище и устойчивее к изменениям

---

Готово. Ниже — **аккуратно оформленный Markdown-текст** для раздела **4.3**, в том же стиле, что и предыдущий 👇

---

# 4.3 — Шаблонизируй то

## Архив проекта

В этом задании мы завершаем начатое во втором разделе. Сейчас в проекте есть две проблемы:

1. отсутствует структура данных для **фильтров**;
2. структура данных **фильма** является неполной.

Начнём со структуры данных фильма.

---

## Мета-информация о фильме (user_details)

В прошлый раз мы «забыли» важный блок `user_details`:

```json
{
  "user_details": {
    "watchlist": false,
    "already_watched": true,
    "watching_date": "2019-04-12T16:12:32.554Z",
    "favorite": false
  }
}
```

Этот блок содержит **мета-информацию** о фильме — служебные и дополнительные данные, относящиеся не к самому фильму, а к взаимодействию пользователя с ним.

Эти данные тоже необходимо генерировать.

---

## Генерация user_details

Переходим в файл:

```
/src/mock/film.js
```

и добавляем код для генерации мета-информации.

### Код генерации данных

*(код из задания)*

Ключевая строка здесь:

```js
watchingDate: alreadyWatched ? getWatchingDate() : null
```

Почему это важно:

* мы генерируем данные, **похожие на реальные**;
* просмотренный фильм **не может существовать без даты просмотра**.

---

## Использование мета-информации

Теперь, когда у нас есть `user_details`, мы можем:

1. вычислить **звание пользователя**;
2. определить **реальное количество фильмов в базе**.

Эти данные выводятся в следующих компонентах:

* `src/view/header-profile-view.js`
* `src/view/footer-statistics-view.js`

---

### Количество фильмов в базе

Количество фильмов должно зависеть от того, **сколько случайных фильмов было сгенерировано**, а не быть захардкоженным.

---

### Звание пользователя

Звание вычисляется **на основе `alreadyWatched`**.

#### Расчёт звания пользователя

*(код / схема из задания)*

При обновлении страницы:

* данные генерируются случайно;
* звание пользователя должно **периодически меняться** (хотя бы через раз-два).

---

## Генерация данных для фильтров

Кроме звания пользователя, на основе мета-информации можно сгенерировать данные для **фильтров**.

Важно понимать:

* такой структуры **нет на сервере**;
* код, который мы сейчас пишем, уже является **боевым**;
* сейчас фильтрация происходит по случайным данным;
* позже этот же код будет работать с реальными данными.

Поэтому подходим к реализации **с максимальной ответственностью**.

---

## Требования из технического задания

В приложении предусмотрены следующие фильтры:

* **All movies** — все фильмы;
* **Watchlist** — фильмы, добавленные в список к просмотру;
* **History** — просмотренные фильмы;
* **Favorites** — фильмы, добавленные в избранное.

### Соответствие фильтров мета-информации

| Фильтр    | Поле в `user_details` |
| --------- | --------------------- |
| Watchlist | `watchlist`           |
| History   | `already_watched`     |
| Favorites | `favorite`            |

С фильтром **All movies** всё просто — он отображает все фильмы без фильтрации.

---

## Функции фильтрации

Создаём файл:

```
src/utils/filter.js
```

и описываем в нём функции фильтрации.

### Функции фильтрации

*(код из задания)*

Почему используется объект, а не отдельные функции:

* не нужно придумывать имена функциям;
* ключи объекта — это названия фильтров;
* значения — функции фильтрации.

---

## Использование фильтров в моках

Теперь в моках мы можем:

1. строить структуру вида:

```js
{
  name,
  count,
}
```

2. использовать готовую функцию `filterFilms()`, которой достаточно передать:

  * массив всех фильмов;
  * нужный тип фильтра.

Функция вернёт **только подходящие фильмы**.

---

## Как это работает

### Схема работы

*(схема из задания)*

* есть массив всех фильмов;
* есть объект с функциями фильтрации;
* на основе мета-информации формируются:

  * фильтры,
  * счётчики,
  * отображаемые данные.

---

## Проверка результата

Запускаем сервер разработки и убеждаемся, что:

* количество фильмов корректно считается;
* звание пользователя меняется при перезагрузке;
* фильтры работают корректно;
* код готов к работе с реальными данными.

---

Готово. Ниже — **аккуратно оформленный Markdown-текст** для раздела **5.1**, в том же стиле и логике, что и предыдущие разделы 👇

---

# 5.1 — Большие перемены (часть 1)

## Архив проекта

### Презентер

В прошлых заданиях в нашем презентере накопилось слишком много обязанностей:

* отрисовка карточек фильмов;
* кнопка **«Показать ещё»**;
* сортировки;
* попап;
* вспомогательные методы.

В результате презентер **разросся**, и ориентироваться в нём стало сложно.
Поэтому в этом задании мы проведём крупный рефакторинг и **разделим презентер на два**:

* **основной презентер** — отвечает за общую логику приложения;
* **дочерний презентер** — отвечает только за список фильмов.

Важно понимать:
это **не два независимых презентера**. Дочерний **зависит от основного**. Разделение условное и нужно исключительно для удобства поддержки кода.

---

## Выделение FilmPresenter

Создаём файл:

```text
/src/presenter/film-presenter.js
```

И переносим туда:

* отрисовку карточки фильма;
* добавление обработчиков событий через механизм подписки.

### Механизм подписки

*(код из задания)*

---

## Изменения в основном презентере

В основном презентере мы удаляем код отрисовки карточки фильма.
Вместо него в методе `#renderFilm()` появляется работа с дочерним презентером:

```js
#renderFilm(film, container) {
  const filmPresenter = new FilmPresenter(container);
  filmPresenter.init(film);
}
```

---

## Проверка после рефакторинга

Запускаем сервер разработки и убеждаемся:

* внешний вид приложения **не изменился**;
* логика осталась прежней;
* мы **не добавили новый функционал**, а только перераспределили код.

---

## Частичный датабиндинг

Теперь, когда у нас два презентера вместо одного, появляется больше пространства для манёвров. Можно начать описывать **логику изменения данных**.

> ⚠️ Важно
> Здесь мы временно **пропускаем работу с попапом** и вернёмся к нему позже.

---

## Как будет работать механизм обновления данных

Проговорим архитектуру вслух:

1. данные хранятся в **модели**;
2. с моделью общается **основной презентер**, не вдаваясь в детали;
3. **дочерний презентер** определяет, *что именно* нужно изменить, и сообщает об этом основному;
4. модель сообщает об изменениях серверу;
5. основной презентер получает сигнал об изменении данных и:

  * обновляет View;
  * делегирует часть работы дочерним презентерам.

Пункты **2** и **4** пока исключаем — мы ещё не работаем с сервером.

---

## Заглушка для обновления данных

В качестве временного решения описываем вспомогательную функцию:

### `updateItem()`

*(код из задания)*

Её задача — **обновлять данные прямо в презентере**.

---

## Обновление данных и перерисовка

После обновления данных нужно заново инициализировать нужный дочерний презентер, чтобы изменения отобразились на странице.

Для этого:

### Запоминаем дочерние презентеры

В методе `#renderFilm()` сохраняем все дочерние презентеры в `Map`:

*(код из задания)*

---

### Обработчик изменения фильма

В методе `#filmChangeHandler()`:

* по `id` фильма находим нужный презентер через `.get()`;
* вызываем у него `.init()` с обновлёнными данными.

*(код из задания)*

---

### Передача обработчика в дочерний презентер

Передаём ссылку на `#filmChangeHandler()`:

*(код из задания)*

А в дочернем презентере вызываем его в обработчиках событий, подменяя нужные данные:

*(код из задания)*

> Обратите внимание:
> мы передаём **полный объект фильма**, но изменяем **только ту часть**, за которую отвечает обработчик
> (например, флаг `watchlist`).

---

## Только один попап

Теперь возвращаемся к попапу. Если посмотреть на количество кода, связанного с ним, становится очевидно:
его тоже стоит вынести в отдельный презентер.

---

## FilmDetailsPresenter

Выделяем код попапа в файл:

```text
/src/presenter/film-details-presenter.js
```

*(код из задания)*

---

## Связь основного презентера и попапа

Информация о фильмах хранится в основном презентере. Чтобы попап «знал», для какого фильма он открыт, используем свойство `#selectedFilm`.

В методе `#addFilmDetailsComponent()`:

```js
#addFilmDetailsComponent = (film) => {
  this.#selectedFilm = film;
  this.#renderFilmDetails();
  document.body.classList.add('hide-overflow');
};
```

Теперь информация о выбранном фильме доступна во всех методах класса.

---

## Отрисовка попапа через презентер

В основном презентере весь код попапа удаляется.
В методе `#renderFilmDetails()` остаётся только работа с презентером попапа:

```js
#renderFilmDetails() {
  const comments = [...this.#commentsModel.get(this.#selectedFilm)];
  this.#filmDetailsPresenter =
    new FilmDetailsPresenter(this.#container.parentNode);

  this.#filmDetailsPresenter.init(this.#selectedFilm, comments);
}
```

---

## Изменение данных в попапе

Работа с изменением данных в попапе **ничем не отличается** от карточек:

* передаём функцию `#filmChangeHandler()` в дочерний презентер;
* вызываем её внутри обработчиков событий.

*(код из задания)*

Не забываем:

* закрытие по клику;
* закрытие по клавише **Esc**.

---

## Требование: один попап

Согласно техзаданию:

> Одновременно может быть открыт только один попап.
> При открытии нового попапа прежний закрывается.
> Несохранённые изменения пропадают.

Для этого добавляем два условия.

### Первое условие

Позволяет переиспользовать существующий экземпляр презентера попапа:

*(код из задания)*

---

### Второе условие

Гарантирует, что перерисовка идёт **взамен существующего попапа**, если он уже был открыт:

*(код из задания)*

---

## Проверка результата

Запускаем сервер разработки и убеждаемся, что:

* данные корректно изменяются;
* карточки и попап синхронизированы;
* одновременно открыт **только один попап**;
* архитектура стала чище и масштабируемее.

---

Если хочешь, дальше можем:

* разобрать **film-presenter vs film-details-presenter** по ролям;
* пошагово пройти **#filmChangeHandler**;
* или сделать схему **взаимодействия презентеров и модели**.


